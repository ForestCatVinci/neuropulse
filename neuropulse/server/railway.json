{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port 8001 & caddy run --config Caddyfile --adapter caddyfile",
    "restartPolicyType": "ON_FAILURE"
  }
}
